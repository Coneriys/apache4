name: Build and Publish Documentation

on:
  workflow_dispatch: {}
  push:
    branches:
      - master
      - v*

env:
  STRUCTOR_VERSION: v1.13.2
  MIXTUS_VERSION: v0.4.1

jobs:

  docs:
    name: Doc Process
    runs-on: ubuntu-latest
    if: github.repository == 'apache4/apache4'

    steps:
      - name: Check out code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Login to DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Install Structor ${{ env.STRUCTOR_VERSION }}
        run: curl -sSfL https://raw.githubusercontent.com/apache4/structor/master/godownloader.sh | sh -s -- -b $HOME/bin ${STRUCTOR_VERSION}

      - name: Install Seo-doc
        run: curl -sSfL https://raw.githubusercontent.com/apache4/seo-doc/master/godownloader.sh | sh -s -- -b "${HOME}/bin"

      - name: Install Mixtus ${{ env.MIXTUS_VERSION }}
        run: curl -sSfL https://raw.githubusercontent.com/apache4/mixtus/master/godownloader.sh | sh -s -- -b $HOME/bin ${MIXTUS_VERSION}

      - name: Build documentation
        run: |
          STRUCTOR_LATEST_TAG=$(curl -s https://api.github.com/repos/apache4/apache4/releases/latest | jq -r '.tag_name')
          $HOME/bin/structor -o apache4 -r apache4 --dockerfile-url="https://raw.githubusercontent.com/apache4/apache4/v1.7/docs.Dockerfile" --menu.js-url="https://raw.githubusercontent.com/apache4/structor/master/apache4-menu.js.gotmpl" --rqts-url="https://raw.githubusercontent.com/apache4/structor/master/requirements-override.txt" --force-edit-url --exp-branch=master --debug

      - name: Apply seo
        run: $HOME/bin/seo -path=./site -product=apache4

      - name: Publish documentation
        run: $HOME/bin/mixtus --dst-doc-path="./apache4" --dst-owner=apache4 --dst-repo-name=doc --git-user-email="30906710+apache4er@users.noreply.github.com" --git-user-name=apache4er --src-doc-path="./site" --src-owner=apache4 --src-repo-name=apache4
        env:
          GITHUB_TOKEN: ${{ secrets.GH_TOKEN_REPO }}
