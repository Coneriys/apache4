services:
  whoami-a:
    image: apache4/whoamitcp
    command:
      - -name
      - whoami-a
      - -certFile
      - /certs/whoami-a.crt
      - -keyFile
      - /certs/whoami-a.key
    volumes:
      - ./fixtures/tcp:/certs

  whoami-b:
    image: apache4/whoamitcp
    command:
      - -name
      - whoami-b
      - -certFile
      - /certs/whoami-b.crt
      - -keyFile
      - /certs/whoami-b.key
    volumes:
      - ./fixtures/tcp:/certs

  whoami-ab:
    image: apache4/whoamitcp
    command:
      - -name
      - whoami-ab
      - -certFile
      - /certs/whoami-b.crt
      - -keyFile
      - /certs/whoami-b.key
    volumes:
      - ./fixtures/tcp:/certs

  whoami-no-cert:
    image: apache4/whoamitcp
    command:
      - -name
      - whoami-no-cert

  whoami-no-tls:
    image: apache4/whoamitcp
    command:
      - -name
      - whoami-no-tls

  whoami:
    image: apache4/whoami

  whoami-banner:
    image: apache4/whoamitcp
    command:
      - -name
      - whoami-banner
      - --banner
